"use client";
import { useState } from "react";
import { Badge, PageHeader, StatCard } from "@/components/ui";
import { rules } from "@/lib/demo-data";
export default function RulesPage(){const [message,setMessage]=useState("");return <><PageHeader eyebrow="规则管理" title="权益与交易规则" description="资格、点数、费用、携伴、儿童、预约和取消均由版本化规则裁决。" action={<button className="button primary" onClick={()=>setMessage("已创建规则草稿，发布前必须通过测试和双人审核。")}>新建规则</button>}/>{message&&<div className="alert success">{message}</div>}<div className="stats-grid"><StatCard label="已发布" value="186" detail="全部为虚拟规则"/><StatCard label="待审核" value="8" detail="2条影响订单" tone="warning"/><StatCard label="测试通过率" value="99.6%" detail="497/499"/><StatCard label="规则冲突" value="1" detail="已触发人工处理" tone="danger"/></div><div className="card table-wrap"><table className="data-table"><thead><tr><th>规则</th><th>版本</th><th>范围</th><th>测试</th><th>状态</th><th>生效时间</th><th>操作</th></tr></thead><tbody>{rules.map(item=><tr key={item.id}><td><b>{item.name}</b><br/><small>{item.id}</small></td><td>v{item.version}</td><td>{item.scope}</td><td><Badge tone={item.tests.includes("22")?"warning":"success"}>{item.tests}</Badge></td><td><Badge tone={item.status==="已发布"?"success":item.status==="草稿"?"neutral":"warning"}>{item.status}</Badge></td><td>{item.effective}</td><td><div className="action-row"><button className="button">测试</button><button className="button">查看Diff</button></div></td></tr>)}</tbody></table></div></>}

