import { Badge, PageHeader, StatCard } from "@/components/ui";
import { benefits } from "@/lib/demo-data";
export default function BenefitsPage(){return <><PageHeader eyebrow="权益中心" title="我的旅行权益" description="资格、点数、费用和使用限制均来自结构化规则引擎。"/><div className="stats-grid"><StatCard label="可用权益" value="6" detail="3个虚拟计划"/><StatCard label="本月已使用" value="2次" detail="全部为模拟记录"/><StatCard label="即将到期" value="1项" detail="未来90天" tone="warning"/></div><div className="benefit-grid">{benefits.map(item=><article className="card benefit-card" key={item.id}><div className="card-top"><div><Badge tone={item.tone as "success"|"warning"}>{item.status}</Badge><h2>{item.name}</h2><p>{item.source} · 有效至{item.expiry}</p></div><strong className="benefit-balance">{item.balance}</strong></div><dl className="rule-details"><div><dt>适用范围</dt><dd>{item.scope}</dd></div><div><dt>携伴规则</dt><dd>{item.companion}</dd></div><div><dt>儿童规则</dt><dd>{item.children}</dd></div><div><dt>预约要求</dt><dd>{item.booking}</dd></div><div><dt>取消规则</dt><dd>{item.cancel}</dd></div></dl><div className="source-line">规则已验证 · 更新时间 2026-07-19</div></article>)}</div></>}

